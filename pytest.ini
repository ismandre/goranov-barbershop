[pytest]
testpaths = tests
addopts = -v --strict-markers --cov=app --cov-report=term-missing
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    concurrent: Concurrent behavior tests
